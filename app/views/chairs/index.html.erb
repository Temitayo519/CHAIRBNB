<div class="container">
  <h1>All Chairs</h1>
  <div class="row">
    <div class="col">
      <div class="cards">
        <% @chairs.each do |chair| %>
          <%#= cl_image_tag("ec02bekuardub6kt7ifl", width: 50, height: 50) %>
          <div class="chair-card">
            <img src="<%= chair.image_url %>" alt="<%= chair.name %>" width="100" height="100">
            <div class="chair-details">
              <p>Name: <%= link_to chair.name, chair_path(chair) %></p>
              <p>Price: £<%= chair.price %></p>
            </div>
            <hr>
          </div>
        <% end %>
      </div>
    </div>
  </div>
<div>
